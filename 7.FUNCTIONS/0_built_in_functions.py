# functions which are available with python module implicitly are known as built-in functions

var="MJ"

# print() is also a built in function
# id() gives the memory address of the variable
print(id(var))

#type( ) gives the data type of the variable

print(type(var))

#eval() gives the mathematical evaluation of an expression
# takes string input
print(eval("10+30-2*90"))